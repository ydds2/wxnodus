# 任务 2：CSV 数值求和

实现一个纯函数 `csvSum(csv)`：输入为 CSV 字符串（每行 `名称,数值`，数值为整数，无表头，无引号转义，行数 ≥1），返回所有数值之和。

示例：
- `csvSum('a,1\nb,2\nc,3')` → `6`
- `csvSum('x,-5')` → `-5`

要求：
- 实现写入 `solution.mjs`（导出 `csvSum`，node 可直接执行）；
- 忽略空行；数值可能为负；禁止第三方包；
- 自测：`node verify.mjs`。
