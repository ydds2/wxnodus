# 任务 3：字母异位词判定

实现一个纯函数 `isAnagram(a, b)`：判断两个字符串是否互为字母异位词（字母重排后相同）。

规则：
- 忽略大小写（'Listen' 与 'Silent' 互为异位词）；
- 忽略空格（'rail safety' 与 'fairy tales' 互为异位词）；
- 其余字符（标点/数字）按普通字符参与比较；
- 禁止第三方包。

要求：实现写入 `solution.mjs`（导出 `isAnagram`）；自测 `node verify.mjs`。
