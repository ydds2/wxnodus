# 任务 1：斐波那契数列

实现一个纯函数 `fibonacci(n)`：返回斐波那契数列第 n 项（0 起始：fibonacci(0)=0，fibonacci(1)=1，fibonacci(2)=1，…）。

要求：
- 实现写入 `solution.mjs`（导出 `fibonacci` 函数，CommonJS/ESM 均可，node 可直接执行）；
- n 为 0..30 的整数；禁止硬编码结果表；禁止依赖第三方包；
- 自测：`node verify.mjs`（会断言 n=0,1,2,10,20,30 六点）。
